apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elasticsearch_4_vCluster.fullname" . }}-test-connection"
  labels:
    {{- include "elasticsearch_4_vCluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elasticsearch_4_vCluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
